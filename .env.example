# AWS credentials are NOT required for Phase 1 (static/plan-based analysis only).
# This file exists as a placeholder for future phases that may need scoped,
# read-only credentials (e.g. IAM policy simulation). Never commit a real .env.

# AWS_PROFILE=
# AWS_REGION=us-east-1
